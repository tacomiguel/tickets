<% content_for :breadcrumb do %>
  <div class="row">
    <div class="col-12">
      <div class="page-title-box d-flex align-items-center justify-content-between">
        <h4 class="mb-0 font-size-18">Departamentos</h4>

        <div class="page-title-right">
          <ol class="breadcrumb m-0">
            <li class="breadcrumb-item"><a href="<%= departaments_path %>">Departamentos</a></li>
            <li class="breadcrumb-item active"><%= @departament.name %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>
<% end %>


<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-body">
        <h4 class="card-title mb-3">Detalle Departamento</h4>
        <hr>

        <table class="table table-details mb-0">
          <tbody>
            <tr>
              <th scope="row" style="width: 150px;">Nombre:</th>
              <td><%= @departament.name || '-' %></td>
            </tr>
            <tr>
              <th scope="row">Estado:</th>
              <td><%= Departament.translate_enum_name(:status, @departament.status) %></td>
            </tr>
            <tr>
              <th scope="row">Registrado:</th>
              <td><%= @departament.created_at.strftime('%d/%m/%Y %I:%M %p') %></td>
            </tr>
            <tr>
              <th scope="row">Actualizado:</th>
              <td><%= @departament.updated_at.strftime('%d/%m/%Y %I:%M %p') %></td>
            </tr>
          </tbody>
        </table>

      </div>
    </div>
  </div>
</div>

<%= link_to 'Editar', edit_departament_path(@departament) %> |
<%= link_to 'Atrás', departaments_path %>
