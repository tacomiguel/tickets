<%= form_with(model: ticket_source, local: true) do |form| %>
  <% if ticket_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_source.errors.count, "error") %> prohibited this ticket_source from being saved:</h2>

      <ul>
      <% ticket_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
